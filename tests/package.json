{
  "dependencies": {
    "chromedriver": "^87.0.7",
    "cross-env": "^7.0.2",
    "geckodriver": "^1.20.0",
    "nightwatch": "^1.5.0",
    "start-server-and-test": "^1.11.5",
    "wait-on": "^5.2.0"
  },
  "scripts": {
    "test": "nightwatch",
    "prestart-server": "cd ../frontend && yarn install && yarn bic && cd ../ && docker-compose build projectx && docker-compose stop",
    "start-server": "docker-compose up",
    "ci": "start-server-and-test start-server http://localhost:8000 test",
    "postci": "cd ../ && docker-compose stop"
  }
}
